<resources>
    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Text color. -->
        <item name="android:textColor">@color/gray</item>
        <!-- Toolbar icon color -->
        <item name="colorControlNormal">@color/white</item>
        <!-- Window background color -->
        <item name="android:windowBackground">@color/white</item>
        <!-- EditText hint color -->
        <item name="android:textColorHint">@color/textHintWhite</item>
    </style>

    <!-- TabLayout style -->
    <style name="MyTabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="android:background">@color/white</item>
    </style>

    <!-- BottomSheetDialog ThemeOverlay style -->
    <style name="ThemeOverlay.App.BottomSheetDialog" parent="@style/ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheet.Modal.Transparent</item>
    </style>

    <!-- BottomSheetModal style -->
    <style name="BottomSheet.Modal.Transparent" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="backgroundTint">@android:color/transparent</item>
    </style>
</resources>