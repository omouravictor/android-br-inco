<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omouravictor.ratesnow">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".framework.application.RatesNowApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_splash_screen_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_splash_screen_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name=".presenter.welcome.WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.App" />
        <activity
            android:name=".presenter.MainActivity"
            android:windowSoftInputMode="adjustResize|adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>