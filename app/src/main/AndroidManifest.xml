<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omouravictor.ratesbr">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.omouravictor.ratesbr.framework.application.RatesBrApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/App">

        <activity
            android:name="com.omouravictor.ratesbr.presenter.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.omouravictor.ratesbr.presenter.welcome.WelcomeActivity"
            android:exported="true" />

        <activity
            android:name="com.omouravictor.ratesbr.presenter.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing" />

    </application>

</manifest>